- name: Install nginx
  yum:
    name: nginx
    state: present

- name: start nginx service
  systemd:
    name: nginx
    state: started
    enabled: yes

- name: Copy Azure devops repo file
  copy:
    src: azurecli.repo
    dest: /etc/yum.repos.d

- name: Install Azure CLI
  yum:
    name: [ "azure-cli" , "libicu" ]
    state: latest

- name: Install azure Devops extension
  shell: az extension add --name azure-devops

- name: Download azure artifacts
  shell: az artifacts universal download --organization "https://dev.azure.com/karanamdine/" --feed "karanamdine" --name "my-first-package" --version "0.0.1" --path .

